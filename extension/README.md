# Lore — VS Code extension

Ask `/why` a technical decision was made and get the real reasoning, with sources.

Requires the Lore backend running (default `http://localhost:8000`). See the
repository root README for backend setup. Configure the URL via the
`lore.backendUrl` setting.
